package student_eduards_puzirevskis.lesson_9_interfaces.level_1_intern;

 interface MyList {
     /*
     public void addToArray(int index, smthg to add);
     метод, который должен добавлять элемент в массив в нужную ячейку, не удаляя ее содержимое, если что-то
     там уже было, а перенося ее на +1

     public void deleteFromArray(int index);
     метод, который должен удалить элемент из массива, перенеся все элементы, которые следуют после, на -1

      public int[] deleteDuplicates();
      метод, который должен удалить дубликаты с массива

      */
}
